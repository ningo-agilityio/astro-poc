---
let { type = 'button', disabled = false } = Astro.props;
---

<button
	class={`py-2 px-4 ${disabled ? 'bg-purple-300 hover:bg-purple-400' : 'bg-purple-500 hover:bg-purple-700'} text-white font-semibold rounded-lg shadow-md focus:outline-none`}
	{type}
>
	<slot />
</button>
